{
  "scripts": {
    "now-build": "next build && next export -o dist",
    "export": "next build && next export"
  },
  "dependencies": {
    "next": "latest",
    "react": "latest",
    "react-dom": "latest"
  }
}
